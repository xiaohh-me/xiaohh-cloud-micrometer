server:
  port: 8080

spring:
  application:
    name: xiaohh-gateway
  profiles:
    active: dev

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0

